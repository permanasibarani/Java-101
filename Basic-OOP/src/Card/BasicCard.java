package Card;

public class BasicCard extends AbstractCard {
    public BasicCard(String cardQuestion, String cardAnswer, CardEnum cardType) {
        super(cardQuestion, cardAnswer, cardType);
    }
}
